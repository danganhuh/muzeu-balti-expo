/**
 * Logo file under `public/images/`. Replace `logo.svg` with your asset
 * (e.g. `logo.png`) and update `LOGO_FILE` here if the name or extension changes.
 */
export const LOGO_FILE = 'logo.svg'

export const logoUrl = `${import.meta.env.BASE_URL}images/${LOGO_FILE}`
